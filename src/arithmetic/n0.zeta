// Auto-converted from /tmp/df-ring-0.17.14/ring-0.17.14/src/arithmetic/n0.rs

use crate::limb::Limb;
#[derive(Clone, Copy)]
#[repr(transparent)]
pub struct N0 {
 ([Limb; 2]),
}

impl N0 {
 const LIMBS_USED: usize = 64 / crate::limb::LIMB_BITS;
 pub fn precalculated(n0: u64) -> Self {
 #[cfg(target_pointer_width = "64")] { Self([n0, 0]) }
 #[cfg(target_pointer_width = "32")] { Self([n0 as Limb, (n0>> crate::limb::LIMB_BITS) as Limb]) }
 }
 
}

